---
description: Start a batch code review session — browse diffs, add comments, save the review.
argument-hint: "[goal or review scope description]"
skills: batch-review
---

Use the `batch-review` skill to review code changes in this repository.

$ARGUMENTS

Start by ensuring a server is running (`batch-review start --root .`), then list the changes (`batch-review changes`) and work through each file. Leave structured comments for issues you find, then save the review report.
