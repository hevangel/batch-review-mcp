# Session: Release Batch Review v0.3.0

## Agent harness
- Harness: Cursor (agent)
- Version: (not recorded)

## Model
- Model: (not recorded)

## Prompt / conversation

### User
Tag and release 0.3.0, release to MCP registry and PyPI too.

### Agent
- Bumped version to **0.3.0** in `pyproject.toml`, `mcpb/manifest.json`, `server.json`, `backend/app.py`, `frontend/package.json`, `frontend/package-lock.json`, README release examples, and workflow example strings.
- Built frontend + wheel + `.mcpb` under **WSL** (Linux) with `uv run --no-project python scripts/build_mcpb.py` to avoid Windows `.venv` on `/mnt/b`; set `packages[0].fileSha256` to the resulting SHA. Ran `scripts/verify_release_mcp_registry.py --tag v0.3.0` in WSL (OK).
- Committed, pushed `main`, pushed tag **`v0.3.0`** to trigger the GitHub **Release** workflow (GitHub Release + assets + optional `uv publish` when `PYPI_API_TOKEN` is set). **`mcp-publisher publish`** must be run locally after `mcp-publisher login github` if not automated.

## Files changed
- Version and registry metadata across the repo; `session_history/2026-04-19_release-v0.3.0.md` — this file

## Reproduction steps
1. Bump versions and `server.json` identifier; obtain Linux MCPB SHA (preflight workflow or WSL/Docker as above).
2. `git commit` / `git push origin main`
3. `git tag v0.3.0 && git push origin v0.3.0`
4. Confirm Release workflow success; run `mcp-publisher publish` for the registry; ensure `PYPI_API_TOKEN` is set for PyPI publish.
