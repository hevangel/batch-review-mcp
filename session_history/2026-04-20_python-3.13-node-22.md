# Session: Require Python 3.13 and Node 22

## Prompt

Bump to Python 3.13 and Node 22 across the repo and CI.

## Changes

- `pyproject.toml` — `requires-python = ">=3.13"`
- `mcpb/manifest.json` — MCP bundle runtime `>=3.13,<4.0`
- `README.md` — Requirements bullets
- `.github/workflows/release.yml` and `mcp-registry-preflight.yml` — `node-version: "22"`, `python-version: "3.13"` on `setup-uv`
- `frontend/package.json` — `engines.node` `>=22.0.0`
- `.python-version` — `3.13` for local `uv` default
- **`frontend/package.json` scripts** — invoke TypeScript and Vite via `node ./node_modules/.../bin/...` so Windows does not rely on broken `node_modules/.bin` shims (e.g. after `npm ci` on Linux/WSL). On Windows, if Rollup reports a missing `@rollup/rollup-win32-x64-msvc`, remove `frontend/node_modules` and run **`npm ci`** again on that OS.

## Files

- See git diff for this commit; `session_history/2026-04-20_python-3.13-node-22.md` — this file
