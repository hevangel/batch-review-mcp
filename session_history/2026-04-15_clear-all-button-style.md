# Session: Clear all button matches Add Comment style

## Prompt
User: the clear all button use the same style as the add comment button.

## Change
- `frontend/src/components/RightPanel/index.tsx` — Clear all uses `px-2 py-0.5 bg-blue-700 hover:bg-blue-600 text-white text-xs rounded` (+ same disabled classes as center panel).
